[project]
name = "frrnet"
version = "0.3.0a4"
description = "Mininet with FRR switches"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mininet>=2.3.0.dev6",
]

[build-system]
requires = ["uv_build>=0.7.17,<0.8.0"]
build-backend = "uv_build"
